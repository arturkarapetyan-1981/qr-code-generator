import QRCodeGenerator from '../components/QRCodeGenerator';

export default function Home() {
  return <QRCodeGenerator />;
}

export const metadata = {
  title: 'QR Code Generator',
  description: 'Generate beautiful QR codes for free',
};